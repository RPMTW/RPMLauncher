name: Updater
description: test

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies: 
  args: ^2.2.0
  path: ^1.8.0

dev_dependencies:
  flutter_lints: ^1.0.4